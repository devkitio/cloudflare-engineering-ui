---
name: cloudflare-engineering-ui
description: 设计、重构或审查开发者工具、基础设施控制台、可观测性页面与高密度数据仪表板，使其呈现 Cloudflare 控制台式的克制工程美学与高扫描效率。适用于用户提到 Cloudflare 风格、工程师美学、运维后台或高密度数据消费；不用于品牌营销页或照搬 Cloudflare 商标资产。
---

# Cloudflare 工程师界面

构建安静、精确、可信的工程界面：信息很多，但认知负担低；视觉有品牌感，但不喧宾夺主。

## 先选择页面模式

- **命令中心/首页**：突出一个主标题、全局搜索和三列快捷入口；数据摘要放在第二屏。允许较大的留白。
- **资源列表/组合视图**：全局侧栏之外，以资源列表为主、右侧上下文栏为辅；资源项用“身份与操作 + 部署与指标”两层紧凑结构，不改成松散磁贴墙。
- **资源详情**：用顶栏面包屑承载资源身份，下面独立放局部页签和右侧主操作；关系重要时让拓扑或依赖图进入首屏，再接 KPI、版本与事件。
- **分析/监控页**：顶部过滤器与时间范围紧邻，随后是横向 KPI 轨道、部署或健康摘要，再进入图表和表格。
- **设置/表单页**：收窄内容列，按任务分段，长页使用右侧章节锚点；不要套用密集图表页的满宽布局。
- **能力受限/套餐门槛**：使用受限宽度的说明布局，明确不可用原因、影响和安全下一步；不要伪装成空仪表板。

不要把首页的宣传型节奏用于数据页，也不要把数据页的紧凑网格强加给设置表单。

## 工作方式

1. 现有项目先审计路由、组件、设计 token、主题和响应式约定；保留产品功能与技术栈。
2. 开始视觉决策前阅读 [references/design-system.md](references/design-system.md)，把其中的观察值映射为项目语义 token，而不是散落硬编码。
3. 构建仪表板、资源列表、图表、表格、筛选器、设置表单或受限状态时，阅读 [references/data-patterns.md](references/data-patterns.md)。
4. 先建立页面壳层和信息层级，再填充组件；优先复用现有组件，只补齐缺失的状态和密度规格。
5. 在真实目标尺寸验证首屏扫描路径、键盘焦点、长中文、空数据、加载、错误与窄屏行为。

## 真实页面观察边界

- 只抽样能代表新布局或新组件的页面；连续样本不再提供新证据时停止，不要求遍历全部产品页面。
- 遇到密钥、凭据、运行时配置或日志正文，只采集计算样式、控件尺寸与脱敏几何统计；禁止读取、截图、导出或持久化正文、变量名、值、完整地址和账户标识。
- 观察保持只读，不点击创建、保存、删除、升级、购买或其他会改变账户状态的操作。

## 不可妥协的视觉原则

- **中性底色承载密度**：画布接近白，卡片用白色或极浅灰；橙色只做品牌签名，蓝色承担主要操作、选中和主数据系列。
- **边界优先于阴影**：用 1px 低对比边线组织区域；阴影仅用于浮层或极轻抬升。禁止大面积渐变、玻璃拟态和发光效果。
- **靠对齐制造密度**：统一列线、基线、行高和数字宽度；不要单纯缩小文字或压扁点击目标。
- **短文案、强层级**：标签短而具体，数值和单位一眼可读，辅助说明退到次级色；避免重复解释和装饰性副标题。
- **状态色有语义**：红色只表示危险/下降，绿色只表示成功/改善，橙色表示警告；趋势必须同时有箭头、文本或符号，不能只靠颜色。
- **圆角克制**：普通卡片 8px，小控件 6px，搜索框/横幅 12px；胶囊只用于标签、键盘提示和真正的二元状态。
- **动效服务反馈**：120–180ms 的颜色、边线和位移过渡即可；尊重 `prefers-reduced-motion`，不做持续漂浮或滚动炫技。

## 工程与内容约束

- 默认字体使用 `Inter Variable`/系统无衬线；代码、版本号、哈希和固定宽数字可用 `Paper Mono`/系统等宽字体。
- 用户可见文本跟随项目语言；中文界面不要为了“技术感”强行混入英文。
- 按钮、标签和数值加载时保持固定几何尺寸，避免文案变化导致布局抖动。
- 长选项列表提供搜索；表格在窄屏横向滚动，不把关键列压成不可读宽度。
- 图表系列控制在可辨识数量内，并提供图例、单位、时间范围和可访问的文本摘要。
- 不复制 Cloudflare Logo、产品名、插画或账户数据；学习其信息组织和视觉语法，而不是伪装成官方界面。

## 完成门

- 首屏能在 5 秒内回答：我在哪里、当前范围是什么、系统是否健康、下一步能做什么。
- 关键操作、选中项、错误和加载状态无需依赖悬停即可识别。
- 360px、768px、1280px 与 1920px 下无非预期溢出；数据表允许受控横向滚动。
- 文本与背景达到 WCAG AA；焦点环清晰，图表状态不只依赖颜色。
- 与现有产品 token、组件和主题兼容，没有为追求风格而重写无关逻辑。

